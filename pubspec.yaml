name: whats_on_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.5 #routing & sizing aja tuk sementara
  google_fonts: ^6.0.0 #alternatif font style - connection must on
  iconly: ^1.0.1
  syncfusion_flutter_core: ^23.1.36 #untuk support custom slider
  syncfusion_flutter_sliders: ^23.1.36
  calendar_date_picker2: ^0.5.3 # date picker range
  intl: ^0.18.1 # format date
  simple_ripple_animation: ^0.0.7 # map ripple effect

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/